# <repo-name> — Claude Code Context

Read `~/.config/ai/context.md` for machine specs and coding preferences.
Read `~/.config/ai/projects/<repo-name>.md` for project memory and history.

---

## What this project is

<!-- One paragraph: what does it do, who uses it, what problem does it solve? -->

## Architecture

```
<!-- Paste your directory tree here, with a one-line description per folder -->
src/
  components/    # React UI components
  lib/           # Shared utilities
  api/           # Backend API handlers
tests/           # pytest / jest test suite
```

## Key rules for this session

<!-- Add project-specific rules that override or extend the global context -->

1. <!-- e.g.: Never auto-commit. Always ask before running destructive commands. -->
2.
3.
